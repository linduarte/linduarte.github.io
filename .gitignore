# .gitignore - repository-specific ignores

# Python virtual environment
.venv/

# Byte-compiled / caches
__pycache__/
app/__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/

# Local test artifacts created by test runs / CI / debugging
failure_*.png
failure_*.html
failure_login.png
failure_logout.png

# Generated reports and test artifacts (keep these out of the repo)
reports/
reports/**
tests/artifacts/
tests/artifacts/**

# Tooling / lock files / small local files
.python-version
uv.lock

# If you need to ignore specific large images, add them explicitly here.
# e.g. app/static/images/large-file.webp

# CI and local audit artifacts
reports_download/
reports_download/**
reports_download_ci/
reports_download_ci/**
reports_download_ci_latest/
reports_download_ci_latest/**
reports_download_ci_latest_run/
reports_download_ci_latest_run/**
reports_local/
reports_local/**
server.log
tmp/

# Node dependencies (don't commit node_modules)
node_modules/

# Ignore generated responsive image variants produced by CI or local generators
app/static/images/*-150.webp
app/static/images/*-300.webp
app/static/images/*-600.webp
app/static/images/*-100.webp
app/static/images/*-200.webp
app/static/images/*-400.webp
app/static/images/*-800.webp

# Backups created by tooling
tmp/backups-*

# Ignore backup files created by the HTML normalization scripts
**/*.bak

# CI / run logs and error artifacts
*.log
gh-run-*.log
*.errors.txt
gh-run-*.errors.txt
gh-run-*.audit_context.txt
gh-run-errors-summary.txt

# Local temporary/test helpers and generated debug files
pr13.githubscript.matches.txt
tmp_html_check.py
extract_run_errors.ps1
watch_pr_runs.ps1

# Local script helpers created during editing/maintenance (do not track)
scripts/_print_lines_helper.py
scripts/_print_range.py
scripts/commit_html_only.ps1

# Ignore all local helper scripts (decided to keep them untracked)
scripts/

# Build output: generated distribution files
dist/
artifacts/

# Ignore build output directory
build/
